interface:
  display_name: "Axios Compromise Scan"
  short_description: "Scan manifests and lockfiles for compromised axios incident versions"
  default_prompt: "Use $axios-compromise-scan to search a repo or machine for compromised package versions."
